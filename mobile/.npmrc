# React Native는 flat한 node_modules 구조가 필요합니다
node-linker=hoisted

# public hoist pattern (React Native 호환성)
public-hoist-pattern[]=*

# Metro bundler 호환성을 위해 모든 패키지를 루트로 hoist
shamefully-hoist=true

# React 중복 설치 방지 - mobile 디렉토리에서는 자체 React 사용
prefer-workspace-packages=false
